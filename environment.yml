name: uk_demand
channels:
  - conda-forge
  - nodefaults
dependencies:
  # defaults
  - click
  - kaggle
  - python>=3.8
  - pip
  - pre-commit
  - pytest>=6  # Adds --import-mode option
  - pytest-cov
  - setuptools-scm
  - holidays
  # data
  - pandas
  - numpy<2
  - dask-ml
  - ydata-profiling
  # interactive
  - ipykernel
  # plots
  - matplotlib
  - nbformat
  # ml
  - glum
  - lightgbm
  - scikit-learn
  - dalex
